server {
    listen 80;
    server_name localhost;

    location /forgesteel/ {
        alias /usr/share/nginx/html/forgesteel/;
        try_files $uri $uri/ /forgesteel/index.html;
    }

    # Redirect root to /forgesteel/
    location = / {
        return 301 /forgesteel/;
    }
}
